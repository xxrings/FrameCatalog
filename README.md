# FrameCatalog
Portland frame catalog

## Local preview

The easiest way to preview the site locally on Windows is:

1. Double-click `open-preview.bat`
2. Your browser should open automatically to `http://127.0.0.1:4173`

If you want to start the server without auto-opening the browser, use `start-preview.bat`.
